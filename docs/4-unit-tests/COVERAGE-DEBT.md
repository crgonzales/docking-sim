# Coverage Debt Ledger

| path | why hard | escape plan |
| --- | --- | --- |
| `apps/web/src/scene/Earth.tsx` (surface + atmosphere GLSL) | Shader raymarch/lighting runs on the GPU; correctness is judged by rendered output, not unit-testable JS. The analytic core is already extracted and tested (`EarthMath.ts`, `sky/atmosphereMath.ts`). | Keep extracting pure math into `sky/atmosphereMath.ts` as it stabilizes; visual regressions are caught by the release screenshot checklist. |
| `apps/web/src/scene/VolumetricClouds.tsx` (billboard GLSL) | Instanced vertex/fragment shaders (fades, FBM shaping, silver lining) have no CPU seam; placement logic IS tested via `sky/cloudPlacement.ts`. | If shaping bugs recur, mirror the fade/size formulas into a pure TS helper with tests, as done for cloud placement. |
| `apps/web/src/scene/Clouds.tsx` (deck GLSL) | Same shader-only limitation; the shared coverage transfer function is tested through `sky/cloudCoverage` consumers. | Covered indirectly by `cloudCoverage` tests; extend those if the deck transfer function grows branches. |
| `apps/web/src/scene/terrain/terrainShaders.ts` (splat + water GLSL) | Slope/altitude splat blending and near-field wave shading run on the GPU; correctness is judged by rendered output. The analytic geometry/classification these shaders consume is already extracted and tested (`terrainWater.ts`: `isWaterHeight`, `geoidSurfacePosition`, `buildWaterPatchGeometry`). | Keep pulling shading math into pure TS helpers as it stabilizes, mirroring the atmosphere/cloud precedent; visual regressions caught by the descent checkpoint screenshots. |
| `apps/web/src/scene/terrain/TerrainPatches.tsx` (r3f node lifecycle + swaps) | React-three-fiber scene wiring (mesh mount/unmount, no-hole swap ordering, culling against real bounding spheres) is only meaningfully exercised against a live GPU/scene graph. The underlying algorithms it orchestrates are unit-tested in isolation: addressing/splitting (`quadtree.ts`), node set transitions (`terrainNodeSet.ts`), tile fetch/LRU/fallback (`tileSource.ts`), and height sampling (`heightField.ts`). | If lifecycle bugs recur (pops, holes, stale disposal), extract the transition/ordering decision into a pure function over `terrainNodeSet` state, as done for the underlying quadtree logic. |
| `apps/web/src/scene/clouds/EveCloudSystem.ts` (real-time scheduling/pause integration) | Orchestrates real GPU producers and view history; generation math and shader behavior have CPU/GPU fixtures, while the full pause lifecycle was checked through actual 60x-play/pause evidence, including ready age-zero lighting and unchanged atlas/history. | Add a focused GPU lifecycle fixture if this scheduling seam grows; preserve the recorded preview/pause regression in the browser checklist. |
| `apps/web/src/flight/FlightMode.tsx` (preset/UI/scene lifecycle) | Pure sizing/storage helpers, owned resources and real composer/PBR shaders have unit/GPU tests; a full React quality switch still requires the live game and asynchronous cloud assets. | Keep the single-tab paused Balanced/High/back evidence comparison for pose/time, actual dimensions and bounded resources; extract lifecycle decisions only if this seam grows. |
