# Changelog - Week 1, 07-08-2026, V. 0.6.0

**Release Date**: Week 1, 07-08-2026 at 11:49
**Version**: 0.6.0 (previously 0.5.0)
**Object**: feat: Phase 5 guided scenario mode, Monte Carlo analysis & mission switch panel
**Code review**: `docs/3-code-review/CR_w1_v0.6.0.md` (review loop, 3 rounds -> APPROVED)

## Changes

- **New workspace package `@docking/scenario`** (pure TS, honesty-invariant enforced): spec §3 schema + strict runtime validator, `FINAL_APPROACH_01` mission data, `scenarioToSimConfig` factory, `ScenarioDirector` (BRIEFING/RUNNING/DEBRIEF phases, beat firing/clearing/escalation per spec §5, sensor-degrade merging, truth-side-only outcome latching), deterministic perfect-operator bot, and `runMonteCarloRun`/`runMonteCarloBatch` with base-100 DOCKED-only scoring.
- **sim-core additions (additive)**: `setNavSource` (MEKF BACKUP dead-reckoning rejects the star tracker), `injectGuidanceFault`/`clearGuidanceFault` (reference freeze, MPC fallback), `injectVelocityBias` (truth-layer one-shot Δv), per-channel sensor `noiseMultiplier`, continuous-rate attitude bias ramp, and new `TelemetryFrame` fields (`nav_source`, `guidance_frozen`, `range_m`, `body_rate_dps`, `corridor_level`).
- **Mission Mode UI**: SANDBOX/MISSION/ANALYSIS app-mode switch, scenario emitter with launch-gated clock and PlayerAction-routed command forwarders, spec §6 switch panel (guarded two-stage controls, GUIDED hint glow, synthesized WebAudio clicks + master-alarm tone), mission clock, briefing/debrief cards with retry, scenario-sourced caution/warning callouts.
- **Monte Carlo Analysis UI**: Web Worker pool with strided global-index sharding and progressive per-run results, run-count clamping, ERROR state surfacing, uPlot histograms for outcomes, grades, prop consumed, and time margin.
- **Testing**: 18 new tests including the 5 spec §7 acceptance tests (determinism, zero-input-never-docks, perfect-operator-docks, schema validation, honesty static check) and regressions for the B1 escalation merge and director guarantee/outcome rules. Browser-verified end-to-end in headless Edge.
- **Spec correction**: `docs/scenario-mode-spec.md` Beat B2 thruster id fixed from nonexistent `B2` to real jet `J6`.
